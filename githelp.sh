git config --global credential.helper store
echo "Run a program by typing its name. If the program is not in the scripts folder, type the name of the folder it is in {case-sensitive}, followed by a forward slash and the script name."
echo "Pulling..."
git pull
# echo "Pushing..."
# git commit -a -m .
# git push --set-upstream origin master
bash
